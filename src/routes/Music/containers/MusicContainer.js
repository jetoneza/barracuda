import { connect } from 'react-redux';

import Music from 'components/Music';

const mapActionCreators = {};

const mapStateToProps = () => ({});

export default connect(mapStateToProps, mapActionCreators)(Music);
